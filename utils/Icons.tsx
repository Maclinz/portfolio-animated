import { Frame, BugPlay, Box, MonitorCheck } from "lucide-react";

export const frame = <Frame />;
export const box = <Box />;
export const bug = <BugPlay />;
export const monitor = <MonitorCheck />;
